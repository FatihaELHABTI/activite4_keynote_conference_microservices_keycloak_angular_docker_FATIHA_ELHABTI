server:
  port: 9999
spring:
  application:
    name: config-service
  profiles:
    active: native # Utilise le système de fichier local pour simplifier
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo # Dossier dans resources
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/